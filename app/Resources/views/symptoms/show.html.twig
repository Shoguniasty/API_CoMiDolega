{% extends 'base.html.twig' %}

{% block body %}
    <h1>Symptom</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ symptom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ symptom.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('symptoms_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('symptoms_edit', { 'id': symptom.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
